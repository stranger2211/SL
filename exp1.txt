Here are the commands extracted from the second PDF:

1. **Creating a LAN network using netkit**:
   ```
   vstart pc1 –eth0=A
   ```

2. **Assign IP addresses to PCs**:
   ```
   ifconfig eth0 192.168.1.11
   ```

3. **Spoofing IP address using iptables**:
   ```
   iptables -t nat -A POSTROUTING -p icmp -j SNAT --to-source 192.168.1.12
   ```

4. **Capturing packets using tcpdump**:
   ```
   tcpdump
   ```

5. **Install arpwatch** (for ARP spoofing):
   ```
   sudo apt-get install arpwatch
   ```

6. **Check the status of arpwatch**:
   ```
   sudo systemctl status arpwatch
   ```

7. **Check log entries for IP and MAC address changes**:
   ```
   tail -f /var/log/syslog
   ```

8. **Change MAC address using ifconfig**:
   ```
   ifconfig eth0 hw ether <new_mac_address>
   ```

These are the key commands from the document for simulating IP and ARP spoofing attacks over a local area network.
